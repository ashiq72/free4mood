export interface IUserInfo {
  _id?: string;
  name: string;
  about: string;
  bio: string;
  location: string;
  title: string;
  description: string;
  image?: string;
  website: string;
  dateOfBirth: string;
}
